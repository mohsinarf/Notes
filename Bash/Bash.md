*Print directory tree*

`tree <directory_name>`

or 

`tree`

`Cat <FileName> -n`

`scp -r -P port# <FileName> Username@IPAddress:/directory/`

`histroy | grep ssh`
```
chmod +x filename
find . -name "*libQtSvg.do*"
Which Bash
#! /bin/bash
```
